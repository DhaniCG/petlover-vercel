## To run this in your local computer, run this command

**First**, installing vite:
<sub>npm install vite@latest</sub>

**Then**, run it with:
<sub>npm run dev</sub>

It won't work if you use <sub>npm start</sub>